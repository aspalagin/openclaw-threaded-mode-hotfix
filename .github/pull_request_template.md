## Change

Describe the exact OpenClaw version and the bundle signatures affected.

## Verification

- [ ] `npm run check:syntax` passes
- [ ] Dry run used the exact supported OpenClaw version
- [ ] No package bundles, backups, secrets, or host-specific paths are included
